namespace Zlodey
{
   public enum GameState { BeforePlay, Play, Win, Lose }
   public enum DistructionState {  Start, Phase1, Phase2, Phase3, End }
   public enum QuestType { Quest1, Quest2, Quest3, Quest4 }
   public enum ZoneType { Green, Red, Blue, None }
}
